Drop the HardGaming brand assets here:

  icon.ico   installer and window icon (Windows, 256x256 recommended)
  logo.png   header mark, transparent background

package.json build.win.icon and build.nsis.installerIcon already point at
icon.ico, so adding the file is all that is needed.
